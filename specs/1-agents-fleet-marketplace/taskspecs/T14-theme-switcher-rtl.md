# T14 — Theme Switcher & RTL

Purpose
- Implement theme switching (light/dark) and robust RTL support for Arabic localization.

Scope
- Data-theme attribute approach, CSS variables, localStorage persistence, account-sync option, and layout mirroring for RTL.

Deliverables
- ThemeToggle component, i18n config, CSS variables file, and RTL QA checklist.

Acceptance Criteria
- Theme preference persists across reloads; RTL pages mirror correctly and pass basic accessibility checks.

Estimated time: 1 day
Owner: Frontend Engineer
Dependencies: T09, T11

# T15 — Scaffold Backend App

Purpose
- Create a starter backend (Node.js NestJS or Fastify) with auth, basic routing, and integration test harness.

Scope
- Project scaffold, env examples, database connection, job worker process for scans/imports, and local dev docker-compose.

Deliverables
- `apps/backend/` scaffold with scripts: dev, migrate, start, test.

Acceptance Criteria
- App boots, connects to dev DB, and exposes health and product API endpoints.

Estimated time: 1–2 days
Owner: Backend Engineer
Dependencies: T04, T06

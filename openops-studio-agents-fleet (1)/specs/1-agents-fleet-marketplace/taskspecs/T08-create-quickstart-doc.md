# T08 — Create Quickstart Documentation

Purpose
- Provide a short, copy-pasteable Quickstart for both sellers (uploading a product) and buyers (importing a product to a platform).

Scope
- Quickstart: ZIP structure checklist, sample `manifest.json`, seller upload CLI example, buyer import CLI example for Gemini/ChatGPT/Claude (stubs).

Deliverables
- `quickstart.md` in the feature docs folder and sample archives in `examples/`.

Acceptance Criteria
- Quickstart reproduces an end-to-end flow in a local dev environment (mocked services) with no external credentials required.

Estimated time: 0.5–1 day
Owner: Docs Engineer
Dependencies: T05, T06, T07
